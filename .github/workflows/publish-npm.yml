name: Publish Forge to GitHub Packages

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: npm-publish-forge
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://npm.pkg.github.com
          scope: '@langersword'
          package-manager-cache: false

      - name: Verify package metadata
        run: |
          test "$(node -p "require('./package.json').name")" = "@langersword/forge"
          test "$(node -p "require('./package.json').version")" != "0.1.0"
          node --check bin/forge-evidence-site.js

      - name: Install and build
        run: npm ci --registry=https://registry.npmjs.org && npm run build

      - name: Run package smoke test
        run: |
          npm pack --dry-run
          test -f public/index.html
          test -f public/styles.css
          test -f public/app.js

      - name: Publish to GitHub Packages
        run: npm publish --registry=https://npm.pkg.github.com
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
